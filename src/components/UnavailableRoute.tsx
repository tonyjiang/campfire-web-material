import React from "react";
import { useRouteError } from "react-router-dom";

export const UnavailableRoute = () => {
  return(
    <div>
      <h1>
        Route Not Available
      </h1>
    </div>
)}
